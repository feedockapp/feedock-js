// features/submit — composing + posting feedback, and the identity gate that
// every public write goes through. Higher layers import only from this barrel.

export { Composer } from "./composer";
export { IdentityPrompt } from "./identity-prompt";
export { SubmitForm } from "./submit-form";
