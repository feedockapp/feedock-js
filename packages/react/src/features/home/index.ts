// features/home — the panel's landing view, composed from the feedback,
// roadmap, and changelog features. Higher layers import only from this barrel.

export { Home } from "./home";
export type { HomeProps } from "./home";
